[package]
name = "llm-runtime-api"
version = "0.1.0"
edition = "2021"
description = "LLM runtime backend API types for ACOS"
license = "MIT OR Apache-2.0"
publish = false

[lib]
path = "src/lib.rs"

[dependencies]
